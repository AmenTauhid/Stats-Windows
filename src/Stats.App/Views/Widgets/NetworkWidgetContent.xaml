<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="Stats.App.Views.Widgets.NetworkWidgetContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Download -->
        <StackPanel Spacing="2">
            <TextBlock Text="Download" FontSize="10" Foreground="#80FFFFFF" />
            <TextBlock
                Text="{x:Bind ViewModel.TotalDownloadSpeed, Mode=OneWay}"
                FontSize="14"
                FontWeight="SemiBold"
                Foreground="#4ADE80" />
        </StackPanel>

        <!-- Upload -->
        <StackPanel Grid.Column="1" Spacing="2">
            <TextBlock Text="Upload" FontSize="10" Foreground="#80FFFFFF" />
            <TextBlock
                Text="{x:Bind ViewModel.TotalUploadSpeed, Mode=OneWay}"
                FontSize="14"
                FontWeight="SemiBold"
                Foreground="#FACC15" />
        </StackPanel>
    </Grid>
</UserControl>
